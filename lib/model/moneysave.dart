import 'dart:collection';

class MoneySave {
  final String title;
  final double money;
  final bool isPay;
  final DateTime date;
  final String userUID;

  MoneySave(this.title, this.money, this.isPay, this.date, this.userUID);

}